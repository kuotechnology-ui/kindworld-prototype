rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // KindWorld uses custom auth (not Firebase Auth), so all collections are open.
    // Restrict this once Firebase Auth is integrated.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
